Launch the Claud-io visual workspace server and open it in the browser.

1. Check if the server is already running on port 3333
2. If not, start it with `node server.js` in the background
3. Open http://localhost:3333/claudio in the browser
4. Tell the user to run `/model sonnet` to switch to sonnet 4.6
